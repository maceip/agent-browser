{
  "name": "agent-browser-extension",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "bun build entrypoints/background.ts --outfile=public/background.js --target=browser && bun build entrypoints/content.ts --outfile=public/content.js --target=browser",
    "watch": "bun build entrypoints/background.ts --outfile=public/background.js --target=browser --watch & bun build entrypoints/content.ts --outfile=public/content.js --target=browser --watch",
    "clean": "rm -rf public/*.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/chrome": "^0.0.268"
  }
}
